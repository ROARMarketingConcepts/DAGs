CREATE TABLE IF NOT EXISTS hotel_bookings(HOTEL varchar(50), IS_CANCELED varchar(50), LEAD_TIME int, ARRIVAL_DATE varchar(50), MARKET_SEGMENT varchar(50), ADR float, RESERVATION_STATUS varchar(50), RESERVATION_STATUS_DATE date);
